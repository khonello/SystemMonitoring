# Shared development and test requirements. Requires Python 3.10+.
#
# Runtime dependencies are split per deployable unit, so no machine installs
# what it does not run:
#
#   Engine  (Linux)   standard library only - nothing to install
#   Admin   (Windows) requirements-admin.txt
#   Client  (Windows) requirements-client.txt
#
# A development box runs the whole test suite, so it needs all of them:
#
#   pip install -r requirements.txt -r requirements-admin.txt -r requirements-client.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
